---
id:
type: tasks
status: in-progress
updated:
phase:
task_count: 0
---

## Tasks
<!-- Table: id | title | module | depends_on | serves
     Ids are max(existing across ALL phases) + 1 — continuous, never reset (D8).
     `module` rather than a type: "api" and "worker" say more than "backend" twice. -->

| id | title | module | depends_on | serves |
|---|---|---|---|---|

## Dependency Order
<!-- The order tasks must be delivered in. Each dependency stalls delivery until the
     other task is MERGED (§7.4.1), so an unnecessary one costs real time. -->
