---
id:
type: decision
status: active         # active | superseded
updated:
area:                  # storage | api | frontend | infra | process | …
supersedes:
superseded_by:
---

# D-NNN — <title>

**When:** <date> · <PHASE-N / TASK-NNN> · <step>
**Decision:** <what was decided, in one or two lines>
**Why:** <the reason — a decision with a reason survives edge cases a bare one does not>
**Constrains:** <what a FUTURE task must now do>

<!-- `Constrains` is the field that earns this file. A fresh agent reads a decision for
     exactly one reason: to know what it must do differently. If you cannot write this
     line, the decision is a note, not a decision — and it belongs in DESIGN.md.

     Append-only: never edited after writing, never deleted. Reversing a decision
     writes a NEW file and flips this one's status to superseded. -->
